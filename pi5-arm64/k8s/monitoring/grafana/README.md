# Grafana Component

This directory contains the deployment files for Grafana, the visualization dashboard.

## Files

- `grafana-deployment.yaml` - Main Grafana deployment configuration
- `grafana-ingress.yaml` - Ingress configuration for accessing Grafana